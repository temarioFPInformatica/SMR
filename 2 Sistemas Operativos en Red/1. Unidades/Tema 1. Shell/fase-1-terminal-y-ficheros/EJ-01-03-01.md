---
Ejercicio: EJ-01-03-01
Fase: 01
Nivel: 03
Nivel_nombre: Medio (N3)
Título: Marko crea, copia, mueve y borra (y aprende a temer a `rm -rf`)
Playlist: B0_Curso_Shell
Vídeo: B0.S.1.3.1 · Marko crea, copia, mueve y borra
---

## EJ-01-03-01 — Marko crea, copia, mueve y borra (y aprende a temer a `rm -rf`)

> [!abstract] Ficha de la práctica
> ### 📌 `B0.S.1.3.1` — Marko crea, copia, mueve y borra (y aprende a temer a `rm -rf`)
> - **Fase 1** (La terminal y el árbol de ficheros) · **Nivel 3** (Medio)
> - **🎬 Playlist:** `B0_Curso_Shell`
> - **📹 Nombre del vídeo:** `B0.S.1.3.1 · Marko crea, copia, mueve y borra`

> [!info] Situación
> Lucía deja una nota: *"Prepara la carpeta de trabajo de la becaria que entra el lunes: una carpeta suya, dentro subcarpetas de proyectos, y una copia de la plantilla de informes."* Es la primera tarea real de Marko con ficheros.

- **🎯 Objetivo real:** crear estructuras de carpetas, copiar, mover y borrar sin llevarte por delante nada que no querías.
- **🧩 Problema que resuelve:** en la terminal **no hay papelera**. Lo que borras, se fue. Hay que trabajar con red.

---

## 📚 Fundamento

> [!info] Antes de empezar: crear es fácil, borrar es definitivo
> | Comando | Qué hace | Cuidado con |
> | :--- | :--- | :--- |
> | `mkdir carpeta` | crea una carpeta | falla si el padre no existe |
> | `mkdir -p a/b/c` | crea **toda la cadena** | ninguno, es el bueno |
> | `touch fichero` | crea un fichero vacío | también actualiza la fecha si ya existe |
> | `cp origen destino` | copia | **necesita `-r` para carpetas** |
> | `mv origen destino` | mueve **y también renombra** | sobrescribe sin avisar |
> | `rm fichero` | borra | **no hay papelera** |
> | `rm -r carpeta` | borra carpeta y contenido | idem |
> | `rm -rf carpeta` | lo anterior, **sin preguntar nada** | el comando más peligroso del sistema |
>
> Que `mv` sirva para renombrar sorprende al principio: mover un fichero al mismo sitio con otro nombre **es** renombrarlo.

> [!tip] Idea clave
> `rm -rf` es una **trituradora industrial**. No pregunta, no hay bandeja de recuperación, y va muy rápido. Los profesionales la usan; la usan **mirando dos veces la ruta** antes de darle a Enter.

> [!warning] El error nº1: `rm -rf` con la ruta mal escrita
> Un espacio de más y la ruta cambia de significado. Estas dos no son lo mismo:
>
> ```
> sudo rm -rf /srv/samba/departamentos
> sudo rm -rf /srv/samba /departamentos
> ```
>
> La segunda borra `/srv/samba` **entero** y luego busca `/departamentos`.
>
> **La costumbre que te salva:** escribe primero `ls -ld` con esa misma ruta. Si `ls` te enseña lo que esperabas, cambia `ls -ld` por `rm -rf`. Si `ls` te enseña otra cosa, acabas de esquivar un desastre.

> [!example] Vocabulario de esta práctica
> - **`mkdir -p`:** crea la cadena entera de carpetas, sin quejarse si alguna ya existe.
> - **`touch`:** crea un fichero vacío o actualiza su fecha.
> - **`cp -r`:** copia recursiva: hace falta para carpetas.
> - **`mv`:** mueve y renombra. Es el mismo comando.
> - **`rm -rf`:** borra sin preguntar y sin papelera. Respeto absoluto.

> [!info] 🔗 Dónde lo vas a usar
> En **Boochan Fase 6** montas el árbol con `sudo mkdir -p /srv/samba/departamentos/facturacion`, y en la **Fase 2** el material te hace ejecutar `sudo rm -rf /etc/samba/ /var/lib/samba/` para purgar Samba. Ese `rm -rf` de la Fase 2 lo vas a escribir tú. Más vale que lo entiendas antes.

---

## 📹 Grabación de esta práctica

> [!important] Obligaciones de grabación (LÉEME — es igual en TODAS las prácticas del curso)
> Esta práctica se **graba entera con OBS**, de principio a fin. No es un repaso al final: quiero ver **cómo lo haces tú**.
> 1. **Prepárate primero** (Paso 0): léete el ejercicio entero y ten a mano OBS y tu identificación.
> 2. **Arranca OBS y, nada más empezar, PRESÉNTATE:** *"Hola, me llamo [Nombre], soy alumno de 2.º SMR, y en este vídeo voy a hacer la práctica B0.S.1.3.1 — Marko crea, copia, mueve y borra."* Y **muestra en pantalla algo que demuestre que eres tú** (tu **Teams**, tu correo `@alu.edu.gva.es` o tu perfil de GitHub). Di **qué vas a hacer**.
> 3. **Graba todos los pasos** sin cortes, hablando lo que haces.
> 4. **Timestamps SIEMPRE** en la descripción del vídeo: `00:00 Presentación` y **uno por cada paso** (`mm:ss`).
> 5. **Al terminar:** nombra el vídeo **`B0.S.1.3.1 · Marko crea, copia, mueve y borra`** y súbelo a tu playlist de YouTube **`B0_Curso_Shell`** (como **"No listado"**).
> 6. **Una sola entrega:** este curso **no se duplica casa/centro**. Grabas y subes el vídeo **una vez**.

---

## 🛠️ Procedimiento

> [!example] Paso 0 — Prepárate, ABRE TUS APUNTES y empieza a grabar
> 1. **Léete el ejercicio entero** antes de tocar nada, para no perderte a mitad del vídeo.
> 2. ✍️ **Abre tu entrada de apuntes**, vacía y con la estructura puesta, **antes de empezar**:
>    ```
>    00_Apuntes/Trimestre_1/B0_Curso_Shell/shell-1.3.1-crea-copia-mueve-y-borra.md
>    ```
>    La plantilla y qué va dentro, en **[📦 Entregables](../02_ENTREGABLES.md)**. **Se abre ahora, no al terminar.**
> 3. **Arranca tu VM de Ubuntu Desktop** y abre la terminal (`Ctrl` + `Alt` + `T`).
> 4. **Arranca OBS** y pulsa **"Iniciar grabación"**. A partir de aquí, **todo queda grabado**.
> 5. **Preséntate** (mira la caja de grabación de arriba): di tu nombre, muestra tu identidad y di qué vas a hacer.

> [!example] Paso 1 — Crea la estructura de la becaria
> ```
> cd ~
> mkdir -p becaria/proyectos/boochan
> mkdir -p becaria/proyectos/interno
> mkdir -p becaria/plantillas
> ls -R becaria
> ```
>
> `-R` lista en árbol. Sin `-p` habrías tenido que crear `becaria`, luego `becaria/proyectos`, luego el resto. Con `-p`, una línea.

> [!example] Paso 2 — Crea ficheros y comprueba qué hace `touch` dos veces
> ```
> touch becaria/plantillas/informe.md
> ls -l becaria/plantillas/
> touch becaria/plantillas/informe.md
> ls -l becaria/plantillas/
> ```
>
> Ha cambiado la **hora**, no el contenido. `touch` sobre algo que ya existe solo actualiza la fecha: **no lo vacía**. Mucha gente cree lo contrario y lo usa con miedo.

> [!example] Paso 3 — Copia: fichero y carpeta
> ```
> cp becaria/plantillas/informe.md becaria/proyectos/boochan/
> ls becaria/proyectos/boochan/
>
> cp becaria/plantillas becaria/plantillas-copia
> ```
>
> El último falla: `omitiendo el directorio`. Para carpetas hace falta `-r`:
>
> ```
> cp -r becaria/plantillas becaria/plantillas-copia
> ls -R becaria/plantillas-copia
> ```

> [!example] Paso 4 — Mueve y renombra con el mismo comando
> ```
> mv becaria/plantillas-copia becaria/plantillas-viejas
> ls becaria/
>
> mv becaria/plantillas-viejas becaria/proyectos/
> ls becaria/proyectos/
> ```
>
> El primero **renombra**. El segundo **mueve**. Mismo comando: `mv` cambia la ruta completa del fichero, y el nombre forma parte de la ruta.

> [!example] Paso 5 — Borra con la costumbre de mirar antes
> Primero mira, luego borra. Siempre en ese orden:
>
> ```
> ls -ld ~/becaria/proyectos/plantillas-viejas
> rm -r ~/becaria/proyectos/plantillas-viejas
> ```
>
> Ahora prueba el borrado interactivo, que pregunta:
>
> ```
> touch ~/becaria/borrame.txt
> rm -i ~/becaria/borrame.txt
> ```
>
> Te pide confirmación. `-i` es tu amigo mientras coges soltura.

> [!example] Paso 6 — Provoca el susto controlado
> Vamos a ver por qué el espacio importa. En una carpeta de pruebas, **no en tu home**:
>
> ```
> mkdir -p /tmp/prueba/datos
> touch /tmp/prueba/importante.txt
> ls -R /tmp/prueba
>
> rm -rf /tmp/prueba /datos
> ls /tmp/prueba
> ```
>
> `/tmp/prueba` ha desaparecido **entera**, incluido `importante.txt`, porque el espacio convirtió una ruta en dos. Si esto llega a ser `/srv/samba`, te quedas sin trabajo.
>
> Que esto lo hayas hecho en `/tmp` y no en el servidor es justo para lo que sirve el laboratorio.

> [!example] Paso 7 — Cierra la grabación y súbela
> 1. **Detén la grabación** en OBS y localiza el archivo del vídeo.
> 2. **Súbelo a YouTube**, a tu playlist **`B0_Curso_Shell`**, como **"No listado"**.
> 3. Nombra el vídeo **`B0.S.1.3.1 · Marko crea, copia, mueve y borra`**.
> 4. En la **descripción**, añade los **timestamps**: `00:00 Presentación` y **uno por cada paso**.

---

## 🚩 Errores y verificación

> [!warning] Errores típicos (evítalos)
> | Error | Qué pasa | Cómo evitarlo |
> | :--- | :--- | :--- |
> | `cp` de una carpeta sin `-r` | `omitiendo el directorio` | Añade `-r`. |
> | `mv` sobre un fichero que ya existe | Lo machaca sin avisar | Usa `mv -i`, que pregunta. |
> | `rm -rf` con un espacio de más | Borras una rama entera del sistema | `ls -ld` con la MISMA ruta antes de borrar. |
> | Creer que `touch` vacía un fichero | Lo evitas por miedo sin motivo | `touch` solo crea o actualiza la fecha. Nunca borra contenido. |

> [!success] ✅ Verificación: ¿está bien hecho?
> - `ls -R ~/becaria` muestra la estructura completa que has creado.
> - Sabes explicar por qué `cp` de una carpeta necesita `-r`.
> - Has renombrado y movido con `mv` y sabes decir en qué se diferencian esos dos usos.
> - Has reproducido el fallo del espacio en `/tmp` y lo has entendido.

> [!question] Comprueba que lo has entendido
> Responde en tus apuntes, **con tus palabras** (copiar del enunciado no cuenta):
> 1. ¿Por qué `mv` sirve a la vez para mover y para renombrar? Explícalo con tus palabras.
> 2. Escribe **la costumbre** que has aprendido para no equivocarte con `rm -rf`, y por qué funciona.
> 3. ¿Qué hace `touch` sobre un fichero que ya existe y tiene contenido?

---

## ✅ Entregables y cierre

- **Entregable vídeo:** `B0.S.1.3.1 · Marko crea, copia, mueve y borra` subido a la playlist `B0_Curso_Shell` (No listado), con presentación al principio y timestamps. **Una sola entrega.**
- **Entregable apuntes:** en `shell-1.3.1-crea-copia-mueve-y-borra.md`, el árbol de `becaria` (salida de `ls -R`) y tu explicación del susto del Paso 6: qué esperabas que pasara y qué pasó.
- **Criterio de éxito:** creas estructuras con `mkdir -p` y borras con la costumbre de `ls -ld` previo.

> [!summary] 🎓 Qué has aprendido en este ejercicio
> - A crear estructuras con `mkdir -p` y ficheros con `touch`.
> - Que `cp` de carpetas necesita `-r`, y que `mv` mueve **y** renombra.
> - Que en la terminal **no hay papelera**.
> - La costumbre profesional: `ls -ld` con la ruta antes de cualquier `rm -rf`.
>
> **Siguiente:** [EJ-01-03-02](EJ-01-03-02.md) — Marko edita ficheros con nano (y hace copia antes).
